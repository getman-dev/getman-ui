/** BodyEditorTheme — slots for the request body editor. */
import type { ThemeSlot } from '../slot';

export interface BodyEditorTheme {
  fileInput:    ThemeSlot;
  formatButton: ThemeSlot;
  textarea:     ThemeSlot;
}